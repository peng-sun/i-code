# single line if statement
if [ "$fname" = "a.txt" ] || [ "$fname" = "c.txt" ]; then echo yes; fi

# single line while loop
while true; do curl -X POST 'http://localhost:9031/tasks/bdbcache?action=getSequenceNumbers'; sleep 60; don
